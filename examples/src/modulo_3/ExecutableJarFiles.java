package modulo_3;

public class ExecutableJarFiles {

    // You can use the e option of the jar command to specify the entry point of your program
    // jar cvfe MyProgram.jar com.mycompany.mkpkg.MainAppClass file1.class file2.class Main-Class: com.mycompany.mypkg.MainAppClas
    // On Windows the Java Reuntine installer creates a file association for the ".jar" extension that launches the file with the javaw -jar comand. (don't open the shell window)
    // On Mac OS X, the operating system recognizes the ".jar" file extension and executes the Java proram when you double-click a JAR file.

    // Java program in JAR file does not have the same feel as a native application. On Windows, you can use third-party wrapper utilities.
    // That turn JAR files into Windows executables.

}
