package modulo_2;

public class AdditionOfClassIntoPackage {

    // To place classes inside a package, put the name of the package at the top of your source  file.
    // Before the code that defines the classes in the package.

    // If you don'tput a package statement in the source file, then the classes in that source
    // then the source file belong to the unnamed package.

    // You must compile and run classes from the base directory.

}
