package modulo_2;

public class Packages {

    // Java allows you to group classes in a collection called a package.
    // Packages are convenient for organizing your work and for separating yourwork from code libraries provided by others.

    // Guarantee the uniqueness of a class names.
    // If are two classes with the same name (two programmers decided to build) there's no conflict.
    // In fact, to absolutely guarantee a unique package use an Internet domain name com.horstmann.corejava.Employee

    // java.util and java.util.jar have nothing to do with each other.

}
