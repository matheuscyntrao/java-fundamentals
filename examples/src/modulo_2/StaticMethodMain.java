package modulo_2;

public class StaticMethodMain {

    // static <T> void requireNonNull(T object)
    // static <T> void requiteNonNull(T object, String message)
    // static <T> void requireNonNull(T object, Supplier<String messageSuplier>
    /*
        If object is null, these methods throw a NullPointerException with no message or the given message.
     */
    // static <T> T requireNonNullElse(T object, T defaultObject)
    // static <T> T requireNonNullElseGet(T object, Supplier<T> defaultSuplier)
    /*
        Returns object if it is not null, or the default object if object is null.
     */

}
